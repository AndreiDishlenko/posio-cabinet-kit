<?php

/*
|--------------------------------------------------------------------------
| Шаги после регистрации
|--------------------------------------------------------------------------
|
| Тот же файл и те же ключи, что в posio.cabinet (config/cabinet_onboarding.php),
| поэтому перенесённый код читает их без правок. Сливается с одноимённым конфигом
| хоста (mergeConfigFrom): хост переопределяет только нужные ключи.
|
| В пакете все шаги выключены — регистрация заканчивается созданием пользователя.
| Тур, разовые подсветки и поздравление с первым чеком перенесены в пакет и
| включаются здесь. Шаг настройки компании, список первых шагов, оповещение
| команды и вехи воронки построены на POS-логике и сервисах posio.cabinet и в пакет
| не входят: ключи оставлены для совпадения с источником, включать их в пакете
| нечему.
|
| Исключение — одобрение регистрации администратором: в пакете оно включено.
|
*/

return [

    // Шаг первичной настройки компании; без него компания появляется только по приглашению.
    'account_setup'          => false,

    // Обучающий тур по меню кабинета. Сценарий свой у каждого проекта.
    'product_tour'           => false,

    // Разовые подсветки того, что само о себе не заявляет.
    'spotlight_hints'        => false,

    // Список первых шагов в углу кабинета.
    'first_steps_checklist'  => false,

    // Поздравление в кабинете с первым пробитым чеком.
    'first_receipt_congrats' => false,

    // Оповещение команды о новом пользователе в служебный Telegram.
    'team_notification'      => false,

    // Вехи воронки «зарегистрирован» и «почта подтверждена» для аналитики пользователей.
    'user_milestones'        => false,

    // Одобрение каждой самостоятельной регистрации администратором с системным правом
    // sysper-users: ему уходит письмо со ссылкой, и до перехода по ней вход в кабинет закрыт
    // (почту подтвердить можно). В пакете включено — кабинет стороннего проекта по умолчанию
    // не открыт любому, кто зарегистрировался. Выключается переменной окружения.
    'registration_approval'  => (bool) env('ONBOARDING_REGISTRATION_APPROVAL', true),

    // Кому уходит письмо со ссылкой одобрения: адреса через запятую. Пусто — всем пользователям
    // с правом sysper-users. Одобрить по ссылке может только вошедший с этим правом.
    'registration_approval_emails' => env('ONBOARDING_REGISTRATION_APPROVAL_EMAILS', ''),

];
