<?php

/*
|--------------------------------------------------------------------------
| Общие параметры публичной части
|--------------------------------------------------------------------------
|
| Файл сливается с одноимённым конфигом хоста (mergeConfigFrom), поэтому хост
| переопределяет только нужные ключи, а остальные остаются отсюда.
|
| `locales` — список локалей публичной части. Одна локаль означает сайт без
| языковых версий: тогда SEO не строит hreflang-альтернативы, а карта сайта
| выдаёт по одному URL на страницу.
|
*/

return [

	'locales' => ['en'],

	// Язык, на который опускаются недостающие PHP-переводы.
	'default_locale' => 'en',

	// Язык посетителя, чей браузер не назвал ни одного из языков сайта.
	'site_fallback_locale' => 'en',

	// Основной язык сайта, чьи страницы живут без языкового префикса (/about), когда
	// остальные — под префиксом (/en/about). Пусто — префикс есть у каждого языка.
	'unprefixed_locale' => null,

	// Язык страницы, объявляемой x-default в hreflang и карте сайта. Пусто — en,
	// если он есть среди языков сайта, иначе первый из них.
	'x_default_locale' => null,

	// Картинка по умолчанию для соцсетей (og:image / twitter:image) и
	// скриншот карточки продукта в микроразметке.
	'default_og_image'        => '',
	'default_og_image_width'  => 1200,
	'default_og_image_height' => 628,

];
