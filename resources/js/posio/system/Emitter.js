import { createEmitter } from '../../emitter.js'

// Шина событий вне компонентов: к ней обращается код, у которого нет экземпляра
// приложения (клиент запросов ставит и снимает паузу интерфейса). Собственная
// реализация вместо внешнего пакета — установка кабинета не должна тянуть за
// собой лишнюю зависимость хоста.
const Emitter = createEmitter()

export { Emitter }
