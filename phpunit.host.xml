<?xml version="1.0" encoding="UTF-8"?>
<!--
    Тесты пакета в проекте-хосте. Запуск из корня хоста: php artisan cabinet-kit:test

    База по умолчанию — SQLite в памяти: каждый тест накатывает миграции заново и
    не касается рабочей базы. Другую тестовую базу задают переменными окружения
    процесса (DB_CONNECTION, DB_DATABASE, ...): значения ниже без force их не
    перебивают. Имя такой базы обязано содержать маркер test — её пересоздают.
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         cacheResult="false"
>
    <testsuites>
        <testsuite name="CabinetKit">
            <directory>tests/Host</directory>
        </testsuite>
    </testsuites>
    <php>
        <env name="APP_ENV" value="testing" force="true"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null" force="true"/>
        <env name="CACHE_STORE" value="array" force="true"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array" force="true"/>
        <env name="QUEUE_CONNECTION" value="sync" force="true"/>
        <env name="SESSION_DRIVER" value="array" force="true"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <env name="DEBUGBAR_ENABLED" value="false"/>
    </php>
</phpunit>
